---
status: in_progress
report_id: <タスク ID をここに>
parent_task_id: <親タスク ID または "standalone">
report_type: debug-report
created_at: <YYYY-MM-DD HH:MM>
---

# Debug Report — <タスク ID>

> 本レポートは debug-toolkit skill の雛形をコピーしたものです。
> 完了時には status を "completed" に変更し、debug-report-gate hook の
> 必須セクションチェックを通過させてください。
> 必須セクションが空欄のまま完了宣言を出すと、Stop hook によって block されます。

---

## 1. 観察事実【必須】

> 症状を客観的に記述（推測なし）。
> ファイル位置・タイミング・再現条件・観測値を含む。
> 「〜のはず」「〜と思われる」は使わない。実測値のみ。

（ここに記述）

---

## 2. 使用した型【必須】

> debug-toolkit skill §2 の型カタログから、本デバッグで使う型を選ぶ。
> **着手時から** このセクションを埋める（最後にまとめて書くのではない）。

選択した型（複数可、該当なしも明示）:

- [ ] 型 1: 全対象 vs 特定対象の発生範囲を最初に切り分ける
- [ ] 型 2: 描写的データから規範的問いへの裏返し
- [ ] 型 3: 全対象共通ソースで特定対象を判定する論理矛盾の罠
- [ ] 型 4: 致命的操作の安全装置を多層化する
- [ ] 型 5: 新機能と既存機能の関係を 4 値で明示する
- [ ] 型 6: 条件分岐の順序依存をデータ駆動テーブルで除去する
- [ ] 型 7: 負の不変条件はテストで明示しないと守れない
- [ ] 型 8: 明示意思 vs 自動推論の階層を保護する
- [ ] 該当する型なし（→ §7 で新規型を提案する義務が発生）

各型をどう活用したかの説明（選択した型ごとに 2〜3 行）:

（ここに記述）

---

## 3. 仮説候補【必須】

> 複数候補を列挙（最低 2 つ。1 つしか思いつかないなら反芻が浅い証拠）。
> 棄却した候補も含める。
> 各候補に対して、検証の優先度と根拠を記述。

候補 1: ...
候補 2: ...
候補 3: ...（任意）

棄却した候補と棄却理由:
- 候補 X: 棄却理由
- 候補 Y: 棄却理由

---

## 4. 検証経過【必須】

> 仮説をどう検証したか、Step ごとに記録。
> investigation skill の反証チェックリストに従って、自分の結論を否定する証拠も探す。

- Step 1: ...
- Step 2: ...
- Step 3: ...

反証チェック:
- [ ] 同じ機能を持つコードパスは他にないか?（全数列挙済み）
- [ ] 実行時ログ / デバッグ出力で結論を裏付けたか?
- [ ] この結論が間違っていた場合、何が真の原因か?（代替仮説を 1 つ以上）
- [ ] 過去の類似調査で「初回結論が間違っていた」事例はないか?

---

## 5. 真因と対処【必須】

### 5-1. 特定された真因

（ここに記述）

### 5-2. 適用した対処

（ここに記述）

### 5-3. 描写的読み（What — 現状はどうだったか）【必須】

> コードはこう動いていた。事実の記述。

（1〜2 行で記述）

### 5-4. 規範的読み（Should — 設計はこうあるべきだった）【必須】

> この症状が起きた以上、設計のここが脆かった。本来こうあるべきだった。
> 同種ミスの再発を防ぐための設計上の示唆。

（1〜2 行で記述）

---

## 6. 型カタログへの還元判定【必須】

> debug-toolkit skill §2 の型カタログを育てるための判定。
> 以下の 3 択から選び、該当セクションを埋める。

- [ ] **A**: 既存の型 N を使って解決した（具体的にどう活用したか §2 にも記述済み）
  - 採用した型: ...
  - 還元提案: なし（既存型で十分カバーされている）

- [ ] **B**: 既存の型では不十分で、新しいパターンを発見した
  - → §7「新規型の提案」を必ず埋める

- [ ] **C**: 既存の型で十分カバーされているが、追加提案なし
  - 該当する既存型: ...
  - なぜ追加提案不要と判断したか: ...

---

## 7. 新規型の提案（§6 で「B」を選択した場合のみ必須）

> 既存の型では不十分なパターンを発見した場合、ここに新規型を提案する。
> debug-toolkit skill §3「型を抽出する型」の 5 ステップに従って抽出する。

### 7-1. 型の名前（30 文字以内、領域非依存、日本語）

（ここに記述）

### 7-2. 抽象的な型説明（3〜5 行）

> 領域横断的に通用する原則の言語化。

（ここに記述）

### 7-3. 具体事例（3〜5 行）

> 今回起きた事例を、固有名（ファイル名・コンポーネント名）を抽象化して記述。
> 「ある管理画面で...」「△△の自動分類機能で...」のような表現に。

（ここに記述）

### 7-4. 教訓（1〜2 行）

> 要するに、こう考えるべきだった、という行動指針。

（ここに記述）

### 7-5. 領域横断性の根拠（最低 2 領域）

> なぜこの型が「汎化したらめっちゃいい」と思うか。
> 別領域での具体的な該当ケースを最低 2 つ挙げる。

- 領域 A: ...
- 領域 B: ...
- 領域 C: ...（任意）

---

## 8. 個人的教訓【任意】

> 型に上げるほどではないが、今回学んだこと。
> 自分の改善ポイント、今回の反省、次回の意識したいこと等。

（ここに記述）

---

## 完了宣言前のチェック

以下を確認してから status を "completed" に変更:

- [ ] §1〜§6 すべてが空でない
- [ ] §6 で "B" を選んだ場合、§7 すべてが空でない
- [ ] 推測を事実として書いていない（「のはず」を使っていない）
- [ ] 反証チェックリストすべてに回答した

すべて OK なら、本レポート末尾の YAML フロントマターの `status: in_progress` を `status: completed` に変更してから完了宣言を出す。

---

## 自己反省の三段構え（任意だが推奨）

> debug-toolkit skill §3-2 の三段構え。
> 新しい失敗から型を抽出する内面の駆動力を働かせる。

- **認める**: ...（思いもよらなかったことを素直に認める）
- **悔しがる**: ...（気付ける余地はあったか、自分の中の伸びしろを明示）
- **次を明示する**: ...（次回からどう探索パターンを改めるか）
